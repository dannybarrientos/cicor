apiVersion: v1
kind: Namespace
metadata:
  name: cicor-commercial
  labels:
    app.kubernetes.io/name: cicor-commercial
    app.kubernetes.io/part-of: cicor
    app.kubernetes.io/component: commercial
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cicor-commercial-config
  namespace: cicor-commercial
data:
  APP_ENVIRONMENT: local
  MODULE_NAME: commercial
  MODULE_TITLE: CICOR Comercial
  API_BASE_PATH: /api/commercial
  API_PUBLIC_BASE_URL: http://cicor.local/api/commercial
  POSTGRES_HOST: cicor-commercial-db-svc
  POSTGRES_PORT: "5432"
  POSTGRES_DB: cicor_commercial_db
  PGDATA: /var/lib/postgresql/data/pgdata
  LOG_LEVEL: INFO
---
apiVersion: v1
kind: Secret
metadata:
  name: cicor-commercial-secret
  namespace: cicor-commercial
type: Opaque
stringData:
  POSTGRES_USER: cicor_user
  POSTGRES_PASSWORD: cicor_pass
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cicor-commercial-db-pvc
  namespace: cicor-commercial
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cicor-commercial-db
  namespace: cicor-commercial
  labels:
    app.kubernetes.io/name: cicor-commercial-db
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: cicor
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cicor-commercial-db
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cicor-commercial-db
        app.kubernetes.io/component: database
        app.kubernetes.io/part-of: cicor
    spec:
      securityContext:
        fsGroup: 999
      containers:
        - name: database
          image: cicor-commercial-db:v3-local
          imagePullPolicy: Never
          ports:
            - containerPort: 5432
              name: postgres
          envFrom:
            - configMapRef:
                name: cicor-commercial-config
            - secretRef:
                name: cicor-commercial-secret
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: cicor-commercial-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: cicor-commercial-db-svc
  namespace: cicor-commercial
  labels:
    app.kubernetes.io/name: cicor-commercial-db
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: cicor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cicor-commercial-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cicor-commercial-api
  namespace: cicor-commercial
  labels:
    app.kubernetes.io/name: cicor-commercial-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: cicor
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cicor-commercial-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cicor-commercial-api
        app.kubernetes.io/component: api
        app.kubernetes.io/part-of: cicor
    spec:
      containers:
        - name: api
          image: cicor-commercial-api:v3-local
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
              name: http
          envFrom:
            - configMapRef:
                name: cicor-commercial-config
            - secretRef:
                name: cicor-commercial-secret
          env:
            - name: PORT
              value: "8000"
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health/startup
              port: http
            failureThreshold: 30
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: cicor-commercial-api-svc
  namespace: cicor-commercial
  labels:
    app.kubernetes.io/name: cicor-commercial-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: cicor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cicor-commercial-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cicor-commercial-ingress
  namespace: cicor-commercial
  labels:
    app.kubernetes.io/name: cicor-commercial-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: cicor
spec:
  ingressClassName: nginx
  rules:
    - host: cicor.local
      http:
        paths:
          - path: /api/commercial
            pathType: Prefix
            backend:
              service:
                name: cicor-commercial-api-svc
                port:
                  number: 80
